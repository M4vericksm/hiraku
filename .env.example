# Base da API usada pelo frontend (build e dev).
# Em dev no browser, localhost funciona:
#   VITE_API_BASE=http://localhost:8000
# No APK/Capacitor, use o IP da LAN do PC (ipconfig → Wi-Fi → IPv4):
#   VITE_API_BASE=http://192.168.x.x:8000
VITE_API_BASE=http://localhost:8000

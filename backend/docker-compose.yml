services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: hiraku
      POSTGRES_USER: hiraku
      POSTGRES_PASSWORD: hiraku
    ports:
      - '5432:5432'
    volumes:
      - hiraku-postgres:/var/lib/postgresql/data

volumes:
  hiraku-postgres:
